# N0 基线核实记录（2026-09-13）

对应指导：`docs/yujin_jiuxu_next_stage_execution_guidance_20260913.md` 第 4 节。

## 1. 工作区与源码身份

| 项 | 观察值 | 命令 |
|---|---|---|
| 实际修复工作树 | `/home/cuihengjia/agent/novel/NS/.worktrees/yujin-unified-remediation` | — |
| 分支 | `codex/yujin-unified-remediation` | `git status --short --branch` |
| HEAD | `0b62f00761c14f8ef33b0cb93a0f4026ba9d17a0` | `git rev-parse HEAD` |
| dirty 状态 | 干净（开工时） | `git status --short` |
| 观察时间 | 2026-09-13T19:54:17+0800 | `date` |

与指导第 2.1 节记载的核对基线一致。

根工作区 `/home/cuihengjia/agent/novel/NS` 仍在 `codex/stage2m-semantic-closure`，有大量其他任务修改；
本轮不进入该分支，不清理、不整批混入其修改。

## 2. Python 环境与源码选择

```
PYTHONPATH="$PWD/src" .conda-env/bin/python -c 'import novel_agent; print(novel_agent.__file__)'
/home/cuihengjia/agent/novel/NS/.worktrees/yujin-unified-remediation/src/novel_agent/__init__.py
Python 3.12.13 | packaged by conda-forge
```

导入的 `novel_agent` 来自本次源码工作树。`.conda-env` 是指向
`/home/cuihengjia/agent/novel/NS/.conda-env` 的符号链接；共享 editable 安装因此不构成
隐含源码选择器，后续所有命令显式设置 `PYTHONPATH="$PWD/src"`。

## 3. 测试失败基线（按节点 ID，不只比较总数）

命令：

```bash
NOVEL_AGENT_FORBID_MODEL_CALLS=true PYTHONPATH="$PWD/src" .conda-env/bin/pytest \
  tests/unit tests/contract -m "not model_required and not integration" --no-cov -q --tb=no -rf
```

结果：**75 failed / 3118 passed / 1 skipped / 1 deselected**，源码 SHA `0b62f00`。

失败节点 ID 清单：`tmp/evidence/n0_baseline_failures_0b62f00.txt`（75 行，已排序）。

与指导第 2.3 节记载的历史数字（75 failed / 3118 passed）逐节点一致，未新增也未有消除。

## 4. 计数口径

- `run`/`task`/`attempt`/逻辑模型请求/provider 请求按运行时分列，本轮 N 阶段不产生新的真实调用。
- v23 的历史 `RUNNING`/`REQUESTED` 残留不在本轮处理，留待 N4 用运行时核对与恢复入口处置。
- 原始响应、业务工件和 `.metadata.json` 在对象清单统计时分列，不以交接文字总数代替完整性核验。

## 5. 本阶段边界

N0 只做只读核对：不修改源码、不重跑 Genesis、不启动模型、不推进 v23。
