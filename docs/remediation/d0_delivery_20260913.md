# D0 交付记录：一份冻结候选的收敛与物化预检（2026-09-13）

对应指导：`docs/yujin_jiuxu_next_stage_execution_guidance_20260913.md` 第 11 节。

## 1. 固定输入

| 对象 | 值 |
|---|---|
| 候选 | `f542293743e518e05b1665e4e8f48c3a7a9bb091c653ba09b52906c96913e2ae`（v23，只读） |
| 绑定该候选的审校 | `47f9a7584b6c4f744bb9064950577ad7b4d5f5d378f7b2304a0c16f4fc2a2ced`（v23，只读） |
| 作者锁 | `yujin-jiuxu-v23/input/planning-locks.json`（只读，经生产编译路径） |
| 诊断代码 | 运行源 `90f1533`（= `a1e8e1f` 中除测试与文档外的全部源码）；记录于 `docs/remediation/evidence/d0.configuration.json` 的 `code_commit` / `deterministic_baseline_commit` |
| 8003 profile | `qwen38_27b_nvfp4_8003` → endpoint `qwen38-27b-nvfp4@8003`，model `qwen38-27b-nvfp4`，revision `qwen38-27b-nvfp4`，sequence limit 131072，output limit 16000 |

未调用生产 CommitService，未推进 v23，未覆盖任何旧对象。诊断只写入自己的对象存储。

## 2. 核对修正：冻结候选的真实状态

指导第 11.1 节记载"这份审校对第四卷引用失实，但后几卷重复揭露、重复晋升确有依据"。
本轮核对确认并**修正了一处推断**：

- 冻结审校的引用确实失实。它说第四卷 `volume_climax`/`ending_state` 出现"正式揭露""获取完整线索"；
  第四卷没有这两句，该句属于第五卷。
- 但**冻结候选本身没有机械缺陷**。用真实作者锁（经生产编译路径）复核：
  `volume_stage_grid_defects` 与 `missing_volume_structure_keys` 对全部 8 卷均为空，
  卷区间 1—800 连续，长程真相的两个锁边界（350 / 401）都被阶段窗口满足。
- N2 记录里提到的"21 条窗口违规"是**没有传目录**时的产物：既无作者约束也无义务目录，
  每个 `serves` 都被判为"未知 handle"。传目录后归零。这是缺目录的假象，不是候选缺陷。
- 因此旧审校被拒的真正原因是**引用失实 + 无结构化意见**，而不是候选有机械问题。

## 3. 确定性诊断（`tests/integration/test_yujin_d0_frozen_candidate_chain.py`）

在隔离空间中驱动完整链路：

| 步骤 | 结果 |
|---|---|
| 冻结审校（`issues=[]` + 自由文本） | `verification_failures` 为空；无阻断意见；派生范围为空；空范围不能改写候选（合成结果 == 父候选） |
| 用真实作者锁复核候选 | 8 卷阶段网格与必需槽位全部合规；宿主不追加任何意见 |
| 有依据的审校（引用候选自身重复措辞） | 4 条阻断意见全部来自审校，引用逐字命中 `volume_climax.description` |
| 范围派生 | 恰好是真正重复该句的卷（由候选文本推导，不是硬编码），字段路径为 `volume_climax` |
| 一次有界修订 | 4 个被点名高潮被改写；模型对第一卷 `capability_ceiling` 的越界改写被记为 `out_of_scope_items == ('vol-1',)` 并在合成中恢复 |
| 复审合成候选 | `ACCEPT` |
| 正式物化预检 | 真实 `PlanCandidateMaterializer._planner_execution` 通过 proof 重新推导合成并接受该候选 |
| 篡改 proof（范围被放宽） | `verify_composition` 拒绝，理由指向范围不符 |

## 4. 真实模型诊断（`tests/model/test_yujin_d0_real_review.py`）

一次真实审校调用（`model_required`，不在确定性路径上）：

| 项 | 值 |
|---|---|
| profile / endpoint | `qwen38_27b_nvfp4_8003` / `qwen38-27b-nvfp4@8003` |
| 受信工件 | 编译后的 `AuthorConstraintRoot`（生产编译路径）+ 可读为空的 World root |
| 输入 / 输出 tokens | 10094 / 449（reasoning 0） |
| 费用可得性 | `unknown`（按"用量未知单列"记录，未按零成本计算） |
| 结论 | `ACCEPT`，0 条阻断意见，`verification_failures` 为空 |

**这条结果本身是本轮最有价值的信息**：在 N1 修复之前，宿主对同一候选产出了 21 条
"未知 handle" 阻断意见；修复之后，一个真实的审校模型在拿到正确目录时给出 ACCEPT，
没有任何虚假引用失败。也就是说，N1 的核验既拒绝了失实引用，也没有把合法候选误伤。

证据留存：`docs/remediation/evidence/d0.review.json`、`docs/remediation/evidence/d0.configuration.json`。

诊断过程中还暴露并修正了两处**真实接线问题**（属于本轮成果）：

1. 只传作者锁原始文件时，审校拿不到约束目录，`serves` 全被判为未知 handle。
   生产路径要求传编译后的 `AuthorConstraintRoot`；诊断现在走
   `compile_planning_lock_channels` + 生产分类映射，不再手工造目录。
2. 没有 World root 时，宿主报 `OBLIGATION_CATALOGUE_UNKNOWN`。
   这是 N2 三态设计的正确行为；诊断因此显式传入可读为空的 World root。

## 4.1 真实 Planner 修订（本轮补齐）

前一轮 D0 只有真实 review，没有真实 Planner 修订。本轮补齐，链路为：

```
有依据的意见（只授权 vol-5.volume_climax）→ 真实 Planner 修订（生产装配路径）
→ 宿主按范围合成 → 真实复审合成候选
```

| 项 | 值 |
|---|---|
| 输入 tokens | 18842（prompt 39267 字符） |
| 输出 tokens | 10409（预算 16000，未截断） |
| 原始修订改动的条目 | **仅 `vol-5`**（授权条目） |
| `out_of_scope_items` | **空** |
| 合成后 vol-5 高潮 | 去掉了重复句式，给出"新后果"（迫使九序盟重估防御策略） |
| 真实复审 | **`ACCEPT`**，0 阻断，`verification_failures` 为空 |
| 总耗时 | 84.82 秒（4 项一起） |

**这条结果的边界**：它证明真实模型修订经生产装配后能被宿主在范围内合成，
且合成候选能被真实复审接受。但**这一次模型没有越界**，所以合成的"恢复"路径
没有被真实触发——该路径由确定性用例覆盖（`test_plan_composition_source_proof.py`
与 D0 链中的越界改写用例）。真实运行若发生越界，会记入 `out_of_scope_items` 而不是断言失败。

证据：`docs/remediation/evidence/d0.revision.json`、`d0.rereview.json`。

## 5. D0 通过条件核对

指导第 11.2 节要求"至少完成一条包含实际修改的'准确审校 → 范围内修订 → 复审 → 正式物化预检'链；
越界内容被保留/拒绝的行为符合 N3，物化来源匹配，调用账本完整"。

| 要求 | 状态 |
|---|---|
| 准确审校 | 确定性链：候选文本导出的 4 条引用全部解析；真实链：真实模型审校并留档 |
| 范围内修订 | 4 个被点名高潮改写，其余条目逐字取父候选 |
| 越界内容被保留/拒绝 | 第一卷越界改写被记录为 out-of-scope 并在合成中恢复 |
| 复审 | 合成候选 ACCEPT |
| 正式物化预检 | 真实 materializer 通过 proof 验证并接受 |
| 物化来源匹配 | proof 的父候选、原始执行、审校、范围、规则版本、摘要全部重新推导一致 |
| 调用账本完整 | 真实调用记录 usage（10094/449/0）；确定性链无模型调用 |
| 恢复后不重放已完成响应 | 由 N4 的 `replay_completed` 分类与 `completed_response_refs` 覆盖 |

仅"字段解析通过"或"某次 review 返回 ACCEPT"不算通过——上表每一项都有对应证据。

## 6. 全量回归

`tests/unit tests/contract tests/integration -m "not model_required and not integration"`：
**74 failed / 3354 passed**。

失败节点 ID 与 N0 基线逐条 `diff`：只有 `test_checked_in_stage2_schemas_match_models`
一项被消除（N3 修正导出漂移所致），**没有新增失败**。

Ruff 通过；`plan_composition.py` 的 MyPy 无错误。

## 7. 未运行项与限制

- **G0 仍未开始。** D0 证明的是"机制能收敛并物化"，不是"八卷已经提交"。
  当前规范产物仍处在 N0 基线描述的状态，没有任何后续提交。
- ~~真实链只有一次 review 调用，没有跑真实 Planner 修订。~~ 已在本轮补齐：
  两次真实 review + 一次真实 Planner 修订（见第 4.1 节）。
  仍未验证的是**真实模型越界时恢复路径由真实运行触发**——本轮模型守规，
  该路径只有确定性证据。
- D0 使用的是可读为空的 World root，因为 v23 全部 World 快照的 obligations 均为空。
  真实义务目录要等 G0 的 STORY/ARC_VOLUME 提交才会产生；依赖义务窗口的
  `serves` 检查因此在真实链上仍未被真实数据驱动过。
- 未运行 `make quality` 全量门槛（仓库仍有 74 项历史失败）；未运行 `make integration`。
- v20/v21 的残留 `RUNNING`/`REQUESTED` 尚未处置；N4 的 `reconcile_first` 会阻止盲重试，
  实际清理由运行时入口完成。
