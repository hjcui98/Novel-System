---
description: Implement the Codex-approved execution slice and write evidence
agent: scoped-implementer
model: opencode-go/deepseek-v4-flash
subtask: false
---

The human has reviewed and approved `.agent/plan.md` by invoking this command.

Read `AGENTS.md`, `.agents/skills/scoped-implementation/SKILL.md`, `.agent/task.md`, and
`.agent/plan.md`. If `.agent/review.md` contains a first `FAIL`, perform the single allowed repair.
Implement only the approved scope, run the declared checks, and write `.agent/implementation.md`.

Additional human arguments: $ARGUMENTS
