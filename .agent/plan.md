# Production content-path repair — implementation plan

Updated: 2026-09-12. User authorised implementation of the source assessment in `review.md`.
Latest user direction: implement promptly and leave tests/acceptance until the end.
The first batch is complete. The user has now authorised implementing every finding in
`docs/production_path_and_skill_audit_20260912.md`. Second-batch implementation and final offline
verification are complete (375 passed). Real models, hybrid services and long runs remain untested.

## Active second batch

- [x] Skills: meaningful cards, mode-aware bounded Writer selection, real Planner selection,
  registered Editor lenses, independent checkpoint assessments, and all 38 method documents.
- [x] Plans: replace superseded scope nodes, entry/invariant/exit semantics, parent/goal ranges,
  obligation identity and persisted fulfillment evidence; preserve reviewer severity.
- [x] Memory: paginated ordinary extraction with coverage closure, bounded World working sets,
  plan-aware retrieval focus and production semantic judging.
- [x] Evidence: preserve source trust in Writer snapshots and restrict Editor gap support.
- [x] Continuity: frozen production resume, completed-call replay, full repair history and
  per-call slice budgeting, unified lookahead boundaries/dependency revalidation and cache bounds.
- [x] Final verification: meaningful regressions, schemas, lint/types, documentation and a
  finding-by-finding completion record. No real model calls, commits, merges or pushes.

## Scope and sequence

1. Memory delivery: preserve grounded evidence and facet attribution; inject the existing model
   Need planner; resolve fair routes per request; pass the real reranker through the selected path.
2. Planning context: supply the bounded accepted parent plan before inquiry and dereference the
   allowed comparison artifacts for Plan Reviewer. Do not expose benchmark Gold or future prose.
3. Plan contracts: preserve executable narrative requirements and author-planned temporal
   obligations; select chapter nodes by scope and ancestor lineage, not shared obligation alone.
   Keep legacy roots readable and keep planned requirements distinct from observed World facts.
4. Writer/Editor: expose critical Memory gaps and require their disposition; check chapter
   requirements with evidence in the final prose; repair length before candidate acceptance.
   Route planning defects to the Planner within the existing bounded runtime topology.
5. Recovery: fix cross-volume window clipping and shrinking-list reconciliation; allow bounded
   local/major repair transitions without reusing invalid work plans.
6. Update affected schemas and regression sources, perform final offline regression and static checks, and update the
   current assessment with implemented scope and explicit verification limits.

## Protected invariants

- Preserve the workflow cleanup already in the working tree; do not commit, merge or push.
- Exact-source evidence, cutoff/scope, root identity, candidate acceptance and atomic settlement
  continue to be enforced by their existing owners.
- Do not silently relabel semantic gaps as supported or turn benchmark diagnostics into product PASS.
- Production capabilities must be wired at the real caller; real-hybrid service selection must
  remain explicit and cannot launch infrastructure or use hidden endpoint assumptions.
- Formal literary quality and endurance remain unverified until a later authorised evaluation.

## Completion notes

All six implementation steps above are implemented. The regression phase additionally caught and
fixed author-source/trusted-context mixing, legacy replan generation reuse, and the missing durable
DRAFT_CANDIDATE to CHAPTER_SET replan transition. The transition requires current-chapter scope,
increasing generation, matching basis and a feedback artifact from the settled predecessor.

See `review.md` and the completion record for final results. Milestone fulfillment reuses stable
Plan identities and evidence-bound World observations. Semantic adequacy of model assessments and
real literary quality remain unverified; major rewrite still uses frozen Memory without a new
interactive retrieval turn.
